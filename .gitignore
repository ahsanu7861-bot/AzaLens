# Secrets
.env
.env.*
!.env.example
backend/.env
backend/.env.*

# Dependencies
node_modules/
backend/node_modules/

# Logs and temporary files
*.log
npm-debug.log*
.DS_Store

# Build and coverage
dist/
build/
coverage/
**/.env
**/.env.*
!.env.example
*.zip

# Local backup archives
*.tar.gz

# Local backup directories
frontend-backup/

# Accidental root lockfile
/package-lock.json

# Shariah live-screening usage ledger (real spend data, not a secret,
# but not meant to be shared across machines/deploys - see
# backend/SHARIAH_COST_SAFETY.md)
backend/storage/halal-terminal-usage.json
backend/storage/halal-terminal-usage.json.lock

# Private Twelve Data trial evidence (may contain licensed response metadata)
backend/storage/twelve-data-trial/
